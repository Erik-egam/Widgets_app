import 'package:flutter/material.dart';

class AppTuturialScreen extends StatelessWidget {
  const AppTuturialScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}